1. **Fix typos in `src/components/RulesAccordion.tsx`**:
   - Change `recommned` to `recommend`.
2. **Standardize capitalization in `src/components/add-question-menu/HotColdSection.tsx`**:
   - Change `hotCold` to `Hot/Cold` in `aria-label` and `title` attributes.
3. **Standardize capitalization in `src/components/NextStepsChecklist.tsx`**:
   - Change `Are You Ready!?` to `Are you ready!?` (sentence case).
4. **Fix accessibility text in `src/components/add-question-menu/RadarSection.tsx`**:
   - Change `radar` to `Radar` in `aria-label` and `title` attributes.
5. **Fix accessibility text in `src/components/add-question-menu/PhotoSection.tsx`**:
   - Change `photo` to `Photo` in `aria-label` and `title` attributes.
6. **Fix accessibility text in `src/components/add-question-menu/ClosestSection.tsx`**:
   - Change `closest` to `Closest` in `aria-label` and `title` attributes.
7. **Fix accessibility text in `src/components/add-question-menu/MeasureSection.tsx`**:
   - Change `measure` to `Measure` in `aria-label` and `title` attributes.
8. **Fix accessibility text in `src/components/add-question-menu/MatchSection.tsx`**:
   - Change `match` to `Match` in `aria-label` and `title` attributes.
9. **Update `src/components/StartScreen.tsx`**:
   - Ensure consistency by ensuring `Start as Hider` and `Start as Seeker` are capitalized as they are. (They are already good).
10. **Run lint and tests**: Ensure `pnpm lint` and `pnpm test` (or relevant equivalents) pass.
11. **Complete pre commit steps to ensure proper testing, verification, review, and reflection are done**.
12. **Submit the PR**: Present the changes using the submit tool.
