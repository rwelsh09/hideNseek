## 2024-05-18 - [Testing Zod Schema Utilities]
**Learning:** Zod does not reliably export `ZodEffects` via the root import in certain module resolution environments, causing `instanceof z.ZodEffects` checks to fail or `ZodEffects` to be undefined. To test fallback branches in utilities like `getSchemaOptions`, an object like `new z.ZodString(...)` or a standard schema (e.g. `z.string()`) should be used rather than trying to construct or cast to a fake `ZodEffects` instance, as the utility defaults to an empty array for any unmatched schemas.
**Action:** When testing internal validation utilities, rely on standard schema types rather than mocking internal AST classes like `ZodEffects` directly.
## 2024-05-18 - [Testing Zod Schema Utilities]
**Learning:** Zod does not reliably export `ZodEffects` via the root import in certain module resolution environments, causing `instanceof z.ZodEffects` checks to fail or `ZodEffects` to be undefined. To test fallback branches in utilities like `getSchemaOptions`, an object like `new z.ZodString(...)` or a standard schema (e.g. `z.string()`) should be used rather than trying to construct or cast to a fake `ZodEffects` instance, as the utility defaults to an empty array for any unmatched schemas. However, the source code should use `.constructor.name` or `._def.typeName` as a fallback to robustly check for ZodEffects without relying on `instanceof`.
**Action:** When testing internal validation utilities, rely on standard schema types rather than mocking internal AST classes like `ZodEffects` directly. When checking for `ZodEffects` in source code, use `obj?.constructor?.name === "ZodEffects" || obj?._def?.typeName === "ZodEffects"` as a safer alternative to `instanceof`.
